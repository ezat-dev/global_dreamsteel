@echo off
REM Copy this file to run-backend.bat and set the real DB_PASSWORD below.
REM run-backend.bat is gitignored so real credentials never get committed.
title SCADA Backend (8081)
set "JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-17.0.20.101-hotspot"
set "PATH=%JAVA_HOME%\bin;%PATH%"
REM global_dream schema - host/port/name default to 192.168.1.45 / 3306 / global_dream
REM (override with DB_HOST / DB_PORT / DB_NAME if the server moves)
set "DB_USERNAME=root"
set "DB_PASSWORD=your-password-here"
cd /d "%~dp0backend"
REM --console=plain: turn off Gradle's progress bar. The bar redraws every second
REM ("<====----> 75% EXECUTING [3m 12s]"), which yanks the console back to the
REM bottom and makes it impossible to scroll up and read the log.
call "%cd%\gradlew.bat" bootRun --console=plain

echo.
echo [backend process exited - see log above. Press any key to close this window]
pause >nul
