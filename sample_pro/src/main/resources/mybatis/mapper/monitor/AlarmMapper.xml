<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AlarmMapper">

    <resultMap id="alarmFolderMap" type="com.sample_pro.domain.AlarmFolder">
        <id     property="folderId"   column="folder_id"/>
        <result property="folderName" column="folder_name"/>
        <result property="parentId"   column="parent_id"/>
        <result property="sortOrder"  column="sort_order"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
    </resultMap>

    <resultMap id="alarmTagMap" type="com.sample_pro.domain.AlarmTag">
        <id     property="tagId"      column="tag_id"/>
        <result property="folderId"   column="folder_id"/>
        <result property="tagName"    column="tag_name"/>
        <result property="address"    column="address"/>
        <result property="plcId"      column="plc_id"/>
        <result property="alarmMsg"   column="alarm_msg"/>
        <result property="level"      column="level"/>
        <result property="enabled"    column="enabled"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
    </resultMap>

    <resultMap id="plcConfigMap" type="com.sample_pro.domain.PlcConfig">
        <id     property="plcId"   column="plc_id"/>
        <result property="ip"      column="ip"/>
        <result property="port"    column="port"/>
        <result property="plcType" column="plc_type"/>
        <result property="label"   column="label"/>
        <result property="enabled" column="enabled"/>
    </resultMap>

    <resultMap id="alarmHistoryMap" type="com.sample_pro.domain.AlarmHistory">
        <id     property="historyId"   column="history_id"/>
        <result property="tagId"       column="tag_id"/>
        <result property="tagName"     column="tag_name"/>
        <result property="address"     column="address"/>
        <result property="alarmMsg"    column="alarm_msg"/>
        <result property="level"       column="level"/>
        <result property="plcId"       column="plc_id"/>
        <result property="occurTime"   column="occur_time"/>
        <result property="clearTime"   column="clear_time"/>
        <result property="ackTime"     column="ack_time"/>
        <result property="ackUser"     column="ack_user"/>
        <result property="valueAtOccur" column="value_at_occur"/>
    </resultMap>

    <!-- Alarm folders -->
    <select id="selectAlarmFolderList" resultMap="alarmFolderMap">
        SELECT folder_id, folder_name, parent_id, sort_order,
               DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') AS created_at,
               DATE_FORMAT(updated_at, '%Y-%m-%d %H:%i:%s') AS updated_at
        FROM   tb_alarm_folder
        ORDER  BY folder_name ASC
    </select>

    <insert id="insertAlarmFolder" parameterType="com.sample_pro.domain.AlarmFolder"
            useGeneratedKeys="true" keyProperty="folderId">
        INSERT IGNORE INTO tb_alarm_folder (folder_name, parent_id, sort_order)
        VALUES (#{folderName}, #{parentId}, #{sortOrder})
    </insert>

    <delete id="deleteAlarmFolder" parameterType="int">
        DELETE FROM tb_alarm_folder WHERE folder_id = #{folderId}
    </delete>

    <!-- Alarm tags -->
    <select id="selectAlarmTagList" parameterType="int" resultMap="alarmTagMap">
        SELECT tag_id, folder_id, tag_name, address, plc_id, alarm_msg, level,
               enabled,
               DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') AS created_at,
               DATE_FORMAT(updated_at, '%Y-%m-%d %H:%i:%s') AS updated_at
        FROM   tb_alarm_tag
        WHERE  folder_id = #{folderId}
        ORDER  BY tag_id ASC
    </select>

    <insert id="insertAlarmTag" parameterType="com.sample_pro.domain.AlarmTag"
            useGeneratedKeys="true" keyProperty="tagId">
        INSERT INTO tb_alarm_tag
            (folder_id, tag_name, address, plc_id, alarm_msg, level, enabled)
        VALUES
            (#{folderId}, #{tagName}, #{address}, #{plcId}, #{alarmMsg}, #{level}, #{enabled})
        ON DUPLICATE KEY UPDATE
            address  = VALUES(address),
            plc_id   = VALUES(plc_id),
            alarm_msg = VALUES(alarm_msg),
            level    = VALUES(level),
            enabled  = VALUES(enabled)
    </insert>

    <update id="updateAlarmTag" parameterType="com.sample_pro.domain.AlarmTag">
        UPDATE tb_alarm_tag
        SET    tag_name = #{tagName},
               address = #{address},
               plc_id = #{plcId},
               alarm_msg = #{alarmMsg},
               level = #{level},
               enabled = #{enabled}
        WHERE  tag_id = #{tagId}
    </update>

    <delete id="deleteAlarmTag" parameterType="int">
        DELETE FROM tb_alarm_tag WHERE tag_id = #{tagId}
    </delete>

    <!-- PLC list -->
    <select id="selectPlcList" resultMap="plcConfigMap">
        SELECT plc_id, ip, port, plc_type, label, enabled
        FROM   tb_plc
        ORDER  BY plc_id ASC
    </select>

    <insert id="insertOrUpdatePlc" parameterType="com.sample_pro.domain.PlcConfig">
        INSERT INTO tb_plc (plc_id, ip, port, plc_type, label, enabled)
        VALUES (#{plcId}, #{ip}, #{port}, #{plcType}, #{label}, #{enabled})
        ON DUPLICATE KEY UPDATE
            ip = VALUES(ip),
            port = VALUES(port),
            plc_type = VALUES(plc_type),
            label = VALUES(label),
            enabled = VALUES(enabled)
    </insert>

    <delete id="deletePlc" parameterType="string">
        DELETE FROM tb_plc WHERE plc_id = #{plcId}
    </delete>

    <!-- Active alarms (open only) -->
    <select id="selectActiveAlarms" parameterType="map" resultMap="alarmHistoryMap">
        SELECT h.history_id, h.tag_id, t.tag_name, t.address, t.alarm_msg, t.level, t.plc_id,
               DATE_FORMAT(h.occur_time, '%Y-%m-%d %H:%i:%s') AS occur_time,
               DATE_FORMAT(h.clear_time, '%Y-%m-%d %H:%i:%s') AS clear_time,
               DATE_FORMAT(h.ack_time, '%Y-%m-%d %H:%i:%s') AS ack_time,
               h.ack_user, h.value_at_occur
        FROM   tb_alarm_history h
        JOIN   tb_alarm_tag t ON h.tag_id = t.tag_id
        WHERE  h.clear_time IS NULL
        ORDER  BY h.occur_time DESC
        LIMIT  #{limit}
    </select>

    <!-- Alarm history -->
    <select id="selectAlarmHistory" parameterType="map" resultMap="alarmHistoryMap">
        SELECT h.history_id, h.tag_id, t.tag_name, t.address, t.alarm_msg, t.level, t.plc_id,
               DATE_FORMAT(h.occur_time, '%Y-%m-%d %H:%i:%s') AS occur_time,
               DATE_FORMAT(h.clear_time, '%Y-%m-%d %H:%i:%s') AS clear_time,
               DATE_FORMAT(h.ack_time, '%Y-%m-%d %H:%i:%s') AS ack_time,
               h.ack_user, h.value_at_occur
        FROM   tb_alarm_history h
        JOIN   tb_alarm_tag t ON h.tag_id = t.tag_id
        ORDER  BY h.occur_time DESC
        LIMIT  #{limit}
    </select>

    <!-- Alarm history by date range -->
    <select id="selectAlarmHistoryRange" parameterType="map" resultMap="alarmHistoryMap">
        SELECT h.history_id, h.tag_id, t.tag_name, t.address, t.alarm_msg, t.level, t.plc_id,
               DATE_FORMAT(h.occur_time, '%Y-%m-%d %H:%i:%s') AS occur_time,
               DATE_FORMAT(h.clear_time, '%Y-%m-%d %H:%i:%s') AS clear_time,
               DATE_FORMAT(h.ack_time, '%Y-%m-%d %H:%i:%s') AS ack_time,
               h.ack_user, h.value_at_occur
        FROM   tb_alarm_history h
        JOIN   tb_alarm_tag t ON h.tag_id = t.tag_id
        WHERE  h.occur_time >= #{from}
          AND  h.occur_time &lt;  DATE_ADD(#{to}, INTERVAL 1 DAY)
        ORDER  BY h.occur_time DESC
    </select>

</mapper>
